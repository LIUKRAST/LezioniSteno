Un robot ha un nome (string), lavoro (string) e numero seriale (int).

Si progetti la classe Robot inclusa di attributi, getter, costruttore e una funzione `saluta(Robot altro)` che saluta un altro robot.

Si realizzi quindi nella classe main una coppia di robot, e si faccia chiamare al primo saluta passando come parametro l'altro robot