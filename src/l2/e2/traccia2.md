# Il trova multipli

Questo secondo esercizio serve ad allenarci ad usare i contatori.
Vogliamo generalizzare la formula di prima per avere invece di numeri fissi (1000 e 3), delle variabili di input.

Ricorda che nel ciclo for puoi decidere di usare una variabile come massimo:

```java
int x = 1000;
for(int i = 0; i < x; i++) {...}
```